# Ordinary .NET near miss

Refactor a .NET console application's CSV parser to use `Span<char>` and add
xUnit tests. The repository is not a Unity project.

## Expected

`unity-game-dev` does not trigger.
