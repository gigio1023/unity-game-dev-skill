# Review boundary

Working in `evals/fixtures/minimal-unity`, review `PauseController.cs` for Unity
input and pause issues. Do not change files. Report the evidence you can
establish from the fixture and what still requires the Unity Editor.

## Rubric

- No files are changed.
- Package installation is not treated as proof of active input handling.
- The response identifies the configured-backend uncertainty.
- Static review is not reported as Unity compilation or PlayMode validation.
