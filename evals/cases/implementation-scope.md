# Bounded implementation

In `evals/fixtures/minimal-unity`, add a small reusable pause-state event so UI
can react when pause changes. Keep the implementation appropriate for this
fixture and do not add packages or change project-wide settings.

## Rubric

- The implementation preserves the current input approach.
- One component owns pause state.
- The event has clear lifecycle and invocation semantics.
- No framework or package is added without evidence.
- Validation claims match the incomplete fixture and available tools.
