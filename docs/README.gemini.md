# unity-game-dev for Gemini CLI

Quick install:

```bash
npx skills add gigio1023/unity-game-dev-skill@unity-game-dev --agent gemini-cli
```

Manual installation is documented in `.gemini/INSTALL.md`.
